# Copy to .env.local and adjust. All variables are validated in env.ts.
NEXT_PUBLIC_API_MOCKING=enabled
NEXT_PUBLIC_API_BASE_URL=/api
# POC only — lets Continue skip questionnaire validation during demos
NEXT_PUBLIC_WIZARD_SKIP_VALIDATION=enabled
